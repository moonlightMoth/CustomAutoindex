#include <stdio.h>
#include <math.h>

int main()
{
	double a, b, c;
	printf("Enter <a>x^2 + <b>x + <c> = 0\n");
	scanf("%lf %lf %lf", &a, &b, &c);

	double x1 = (-b + sqrt(b*b - 4*a*c))/(2*a);
	double x2 = (-b - sqrt(b*b - 4*a*c))/(2*a);

	printf("x1 = %lf\nx2 = %lf\n", x1, x2);
	return 0;

}

